# HANDOFF — 2026-09-05 (mid-session, Rex: "handoff")

Read order: this file → lingua-mundi-ops/TICKET-QUEUE-SORTED-2026-09-04.md →
lingua-mundi-ops/QT-TAURI-MIGRATION-2026-09-05.md.

---

## 1. Qt → Tauri 2 migration (Chén Báaxal + Xokbil) — IN PROGRESS

Rex directive: "Replace Qt with the standards we've been using for
multiplatform" + "Chén Báaxal is a sibling of Xokbil, they should look and
feel similarly" + "separate product family, follow Xokbil docs."

### DONE (committed, source-level)
- **Chén Báaxal** (commit ab9f1dd): Tauri 2 scaffold + Rust bridge
  (`src-tauri/` — commands open_mesh/generate_pattern/selftest calling
  chen_baaxal_core directly) + React/TS UI (`ui/` — craft palette, warm
  terracotta/cream, AH font). `cargo check` PASS, **debug binary built**
  (217MB), core 13/13 tests green. Docs updated (README/SPEC say Tauri).
- **Xokbil** (commit 871e392): same scaffold + bridge
  (open_dst/normalize_region_cmd/selftest over xokbil-core) + React UI
  (`ui/` — embroidery palette deep-green/thread, AH font). cargo check PASS,
  **debug binary built** (216MB), core 12/12 green. README updated.

### IN FLIGHT / NEXT
- **Chén Báaxal release .deb**: `tauri build` was running (chen-tauri-release.log)
  but was INTERRUPTED mid-compile (tauri-macros stage) — no release binary yet.
  Command to resume:
  ```
  export PATH="/home/rex/.nvm/versions/node/v24.18.0/bin:$PATH"
  cd projects/chen-baaxal/src-tauri
  ../../../(ui node_modules)/.bin/tauri build   # or: npm --prefix ui i then npx tauri build
  ```
  → produces target/release/bundle/deb/Chén Báaxal deb. Then upload to
  chen-baaxal release (delete-then-upload, replace ChenBaaxal_0.1.0_amd64.deb).
- **Xokbil release .deb**: same, after Chen (its debug deps are warm so
  release will be faster).
- Verify both UIs render (xvfb-run if headless) and look like siblings.
- Remove old Qt remnants after green: chen-baaxal/shell/ (main.cpp,
  CMakeLists, build/), xokbil/app/ (QML), xokbil/rust-feasibility/,
  xokbil/CMakeLists.txt. (Keep until the Tauri deb ships.)

### Key facts for continuation
- Reference scaffold: projects/toscanini-rs/src-tauri (tauri 2, dialog+fs
  plugins, capabilities/default.json, beforeDev/build via npm --prefix).
- Dev ports: toscanini 1420, chen-baaxal 1421, xokbil 1422.
- Icons: chen-baaxal from rhls-hub/icons/chen-baaxal.svg; xokbil generated
  (thread-spool SVG in /tmp — gone now; regenerate from scratch or extract
  from src-tauri/icons pngs).
- Rust: cargo 1.97.1. Cargo deps cache ~1266 crates (mostly warm).
- Each release build takes ~15-25 min on this box.

---

## 2. Font licensing (DONE, Rex directive)

- AH **Next** (Atkinson Hyperlegible Next) is NOT free → archived to
  `~/Descargas/The Vault/Fonts/_archive-nonfree/`.
- Free **Atkinson Hyperlegible** (OFL 1.1, Braille Institute): 4 weights
  (Regular/Bold/Italic/BoldItalic TTF) at
  `~/Descargas/The Vault/Fonts/Atkinson Hyperlegible/` + OFL.txt + README.
- Bundled offline in both new UIs (`ui/public/fonts/` + @font-face).
- RULE: any "Atkinson Hyperlegible" use = these free files, never AH Next.

---

## 3. Distillation / Tlacuatzin (Kaggle) — WATCHING

- Kernel `rexhernndez/tlacuatzin-lora-v1-qwen2-5-coder-1-5b`: v3 pushed
  (no-pip rewrite, plain Trainer+PEFT fp16). Was RUNNING; watcher
  (kaggle-watch-v2/v3) NOT currently alive — recheck status + re-arm:
  ```
  export PATH="/tmp/kaggle-venv/bin:$PATH"
  kaggle kernels status rexhernndez/tlacuatzin-lora-v1-qwen2-5-coder-1-5b
  # re-arm: STATE_DIR=corp/state/detach-runs bash detach-run.sh kaggle-watch-v4 -- bash /tmp/kaggle-watch2.sh
  ```
- On COMPLETE: adapter → GGUF Q4 → llama-swap slot → eval PASS.

---

## 4. Asset parity / naming (state)

- Ledgerly: deb (104MB electron) + exe (101MB) + web zip — FULL desktop now.
- Toscanini: renamed from "Choir Director" everywhere (store + 12 locales +
  launch site). Asset file still choir-director_1.0.0_amd64.deb (Kotlin
  productName) — tied to the toscanini-rs ship decision (H4/M7).
- Store: 18 download links all live, 0 dead. GitHub Pages deploying fine.
- About/CV/Contact pages: fixed + verified clean (EN+ES).

---

## 5. Open threads needing attention

- Kaggle training watcher (above).
- Ledgerly store card: confirm deb link points to new electron deb name
  (Ledgerly_0.1.0_amd64.deb — uploaded OK).
- The 12-item launch queue + sorted ticket queue (see TICKET-QUEUE doc):
  reds = H1 distillation (running), H2 Windows CI (Rex hold), H3 LM API
  public, H4 toscanini-rs verify, H5 platform parity, H6 dataset union.
- Money lane: outreach, 0 sales.

## 6. Safety / rules recap

- Never force-push after failed fetch. Token inline in push, scrub after.
- /tmp wiped between commands. Background jobs via detach-run.sh (escalated).
- Cores (chen_baaxal_core, xokbil-core) must stay untouched/zero-dep.
- AH Next never ships. MyMemory banned for l10n. No polling — self-alerting
  watchers + notify.sh.
